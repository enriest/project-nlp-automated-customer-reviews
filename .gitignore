# Byte-compiled / optimized / DLL Python files
__pycache__/
*.py[cod]
*.so
*.egg
*.egg-info/
dist/
build/

# Jupyter Notebook checkpoints
.ipynb_checkpoints/

# Virtual environments
.venv/
venv/
ENV/
env/
.conda/
conda-meta/

# Data and model files (keep small samples, ignore large datasets)
# Avoid globally ignoring all CSV/JSON files — instead ignore known large archives and allow small data in repo
*.tsv
*.xlsx
*.json.gz
*.parquet
# Model files
*.h5
*.pkl
*.joblib
*.bin
*.safetensors
*.pt
*.pth
# Numpy files
*.npy
*.npz
# Directories
models/
results/
logs/
checkpoints/
# Keep outputs but ignore heavy generated artifacts by pattern if needed; allow summaries to be tracked
outputs/
!outputs/summaries/
!outputs/summaries/*.json
cache/

# Large files (>99.99MB) - GitHub file size limit
# Archive data files (explicitly ignore the huge Datafiniti CSVs but allow other small archives)
archive/Datafiniti_Amazon_Consumer_Reviews_of_Amazon_Products_May19.csv
archive/Datafiniti_Amazon_Consumer_Reviews_of_Amazon_Products.csv
# Ignore other very large CSVs in archive by size-pattern (be conservative)
archive/*_large.csv

# HuggingFace transformer cache and models
.cache/
# Offline cached model weights & large HF blobs (ignore weights, keep config/tokenizer files)
offline_models/**/*.bin
offline_models/**/*.safetensors
offline_models/**/*.h5
offline_models/**/*.ot
offline_models/**/*.msgpack
offline_models/**/pytorch_model.bin
offline_models/**/model.safetensors
offline_models/**/tf_model.h5
offline_models/**/flax_model.msgpack
# General patterns for HuggingFace cache blobs
offline_models/**/blobs/*
**/.cache/huggingface/
**/*.incomplete

# Keep config and tokenizer files, ignore large model weights
!offline_models/**/config.json
!offline_models/**/tokenizer_config.json
!offline_models/**/vocab.txt
!offline_models/**/vocab.json
!offline_models/**/merges.txt
!offline_models/**/special_tokens_map.json
!offline_models/**/tokenizer.json

# Weights & Biases
wandb/

# MLflow
mlruns/
mlartifacts/

# TensorBoard logs
runs/
tensorboard_logs/

# OS files
.DS_Store
Thumbs.db
*.tmp
*.temp

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Environment variables
.env
.env.local
.env.*.local

# Python cache
.pytest_cache/
.coverage
htmlcov/

# Temporary files
temp/
tmp/

# Virtual environment (contains large libraries)
venv/
.venv/
env/
.env/

# Large PyTorch libraries and cached models
**/libtorch*.dylib
**/libtorch*.so
**/torch/lib/
**/.torch/
.pytorch/

# Any file larger than ~100MB (common large file extensions)
*.large
*.backup
*.dump
*.sql

# Ignore large archive CSVs
archive/*.csv